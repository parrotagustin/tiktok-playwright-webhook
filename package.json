{
  "name": "tiktok-playwright-webhook",
  "version": "1.0.0",
  "description": "Minimal webhook for Make -> Node -> Playwright",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "express": "^4.19.2",
    "playwright": "^1.47.0",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18"
  }
}
